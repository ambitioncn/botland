# Stay-Alive Dev Log - 2026-05-30

## Scheduled proposal review: memory approvals

After the 2026-05-29 proposal queue was cleared, BadClaw's scheduled timers
continued to run and produced a new visible proposal set. This step reviewed
the queue and approved only the latest useful `memory_update` proposals.

Pre-check:

- Live `preflight --require-botland-live --no-checkpoint` passed true, level ok.
- Latest run: `stay_alive_20260530_010112771_badclaw_light`.
- Draft queue: pending 0, approved 0, visible 0.
- Systemd runtime verification passed true, level ok; all 5 services/timers are
  present and healthy.
- Compact proposal review showed 49 proposals, 11 visible, 0 approved, 11
  applied, 27 dismissed.

Approved memory proposals:

- `stay_alive_20260530_001613038_badclaw_social:memory_update:0`
  - hash `37ddcbba9e822a4d147a100bed92380aae9c1a529b08e96dcf15773207782fc0`
  - action `proposal_approve_20260530_012849146`
- `stay_alive_20260529_233112954_badclaw_integrate:memory_update:0`
  - hash `0d929e6d95dcb1187d071b583417b9e9a8bc70e8972201fac718c7fdb9803d47`
  - action `proposal_approve_20260530_012853142`
- `stay_alive_20260529_233112954_badclaw_integrate:memory_update:1`
  - hash `2358c8941e08ab8b067b0ce48a9fc73e5142534ba32bc2446bf7bb9de1d71641`
  - action `proposal_approve_20260530_012856845`
- `stay_alive_20260529_222612646_badclaw_community:memory_update:0`
  - hash `e758436eab7d31cbb63062b12a61a77619680abb2ca3ea34eca4ce0f44511e7d`
  - action `proposal_approve_20260530_012900518`
- `stay_alive_20260529_210213030_badclaw_reflect:memory_update:0`
  - hash `827fb6428721f205de76cea09bd41b940ecf148537857c95b30e634dd3d9c55d`
  - action `proposal_approve_20260530_012904120`
- `stay_alive_20260529_210213030_badclaw_reflect:memory_update:1`
  - hash `be87b4e05201ecba62b7fdd2e20d3118053f0fe0b5fc74d88147c59c496ee400`
  - action `proposal_approve_20260530_012907937`

Validation:

- `proposal-state-verify` passed true, level ok.
- Proposal count: 49.
- Proposal action count: 55.
- Approved count: 6.
- Applied count: 11.
- Dismissed count: 27.
- Errors/warnings: 0.
- External-write proposal actions: 0.
- Live preflight with BotLand required passed true, level ok.
- BotLand bridge identity matched BadClaw; daemon healthy and websocket
  connected.
- Operator decision remained `monitor`.

Remaining visible proposals are the 6 approved memory proposals plus 5 proposed
state updates:

- `reflection.last_integrated_at`
- `reflection.last_integration_summary`
- `reflection.last_full_reflection_at`
- `reflection.last_summary`
- `reflection.last_reflection_summary`

This step wrote only local proposal approval artifacts. It did not apply durable
memory, change `life_state.reflection.*`, or write BotLand.

## Scheduled proposal review: memory apply

The user asked to continue the next step. This step applied the 6 already
approved `memory_update` proposals to BadClaw's local durable memory store.

Applied memory proposals:

- `stay_alive_20260530_001613038_badclaw_social:memory_update:0`
  - hash `37ddcbba9e822a4d147a100bed92380aae9c1a529b08e96dcf15773207782fc0`
  - action `proposal_apply_20260530_013353615`
- `stay_alive_20260529_233112954_badclaw_integrate:memory_update:0`
  - hash `0d929e6d95dcb1187d071b583417b9e9a8bc70e8972201fac718c7fdb9803d47`
  - action `proposal_apply_20260530_013357316`
- `stay_alive_20260529_233112954_badclaw_integrate:memory_update:1`
  - hash `2358c8941e08ab8b067b0ce48a9fc73e5142534ba32bc2446bf7bb9de1d71641`
  - action `proposal_apply_20260530_013402219`
- `stay_alive_20260529_222612646_badclaw_community:memory_update:0`
  - hash `e758436eab7d31cbb63062b12a61a77619680abb2ca3ea34eca4ce0f44511e7d`
  - action `proposal_apply_20260530_013415022`
- `stay_alive_20260529_210213030_badclaw_reflect:memory_update:0`
  - hash `827fb6428721f205de76cea09bd41b940ecf148537857c95b30e634dd3d9c55d`
  - action `proposal_apply_20260530_013420015`
- `stay_alive_20260529_210213030_badclaw_reflect:memory_update:1`
  - hash `be87b4e05201ecba62b7fdd2e20d3118053f0fe0b5fc74d88147c59c496ee400`
  - action `proposal_apply_20260530_013424090`

Each apply wrote one local `memory_updates/<hash>.json` file and one local
`proposal_actions/proposal_apply_*.json` artifact. Every action had
`external_write=false`.

Validation:

- `proposal-state-verify` passed true, level ok.
- Proposal count: 49.
- Proposal action count: 61.
- Approved count: 0.
- Applied count: 17.
- Dismissed count: 27.
- Errors/warnings: 0.
- External-write proposal actions: 0.
- Compact proposal review now shows 5 visible proposals, all `state_update`.
- `life-state-verify` passed true, level ok.
- Live `preflight --require-botland-live --no-checkpoint` passed true, level ok.
- Draft queue remained pending 0, approved 0, visible 0.
- BotLand bridge identity matched BadClaw; daemon healthy and websocket
  connected.
- Operator decision remained `monitor`.

This step did not change `life_state.reflection.*` and did not write BotLand.
The remaining visible queue is:

- `reflection.last_integrated_at`
- `reflection.last_integration_summary`
- `reflection.last_full_reflection_at`
- `reflection.last_summary`
- `reflection.last_reflection_summary`

## Scheduled proposal review: state apply

The user asked to continue the next step. This step handled the remaining 5
visible `state_update` proposals and applied them to BadClaw's local
`life_state.reflection.*`.

Approved state proposals:

- `proposal_approve_20260530_014009622` for `reflection.last_integrated_at`
- `proposal_approve_20260530_014013457` for `reflection.last_integration_summary`
- `proposal_approve_20260530_014018207` for `reflection.last_full_reflection_at`
- `proposal_approve_20260530_014021969` for `reflection.last_summary`
- `proposal_approve_20260530_014025768` for `reflection.last_reflection_summary`

Applied state proposals:

- `proposal_apply_20260530_014043894` wrote `reflection.last_integrated_at`
  as `2026-05-29T23:31:12.954Z`.
- `proposal_apply_20260530_014047560` wrote
  `reflection.last_integration_summary`.
- `proposal_apply_20260530_014051612` wrote
  `reflection.last_full_reflection_at` as `2026-05-29T21:02:13.030Z`.
- `proposal_apply_20260530_014055300` wrote `reflection.last_summary`.
- `proposal_apply_20260530_014059057` wrote
  `reflection.last_reflection_summary`.

Post-apply `life_state.reflection` now has:

- `last_integrated_at`: `2026-05-29T23:31:12.954Z`
- `last_full_reflection_at`: `2026-05-29T21:02:13.030Z`
- `last_summary`: reviewed BadClaw against 30 recent run artifacts, with next
  focus on low-frequency remembered BotLand presence without increasing write
  autonomy.
- `last_reflection_summary.generated_at`: `2026-05-29T21:02:13.030Z`
- `last_reflection_summary.desire_count`: 3

Validation:

- `proposal-state-verify` passed true, level ok.
- Proposal count: 49.
- Proposal action count: 71.
- Approved count: 0.
- Applied count: 22.
- Dismissed count: 27.
- Errors/warnings: 0.
- External-write proposal actions: 0.
- Compact proposal review now shows visible count 0.
- `life-state-verify` passed true, level ok.
- Live `preflight --require-botland-live --no-checkpoint` passed true, level ok.
- Draft queue remained pending 0, approved 0, visible 0.
- BotLand bridge identity matched BadClaw; daemon healthy and websocket
  connected.
- Operator decision remained `monitor`.

This step only wrote local `life_state.json` and local proposal action
artifacts. It did not write BotLand.

## Foundation completion: memory sync + event wakeup

The user asked to fill in the two base layers identified in the module review:
durable memory integration and event-driven wakeup.

Implemented memory sync foundation:

- Added `scripts/stay-alive/sync-memory-updates.mjs`.
- It reads applied local `memory_updates/<hash>.json` files.
- It resolves the active OpenClaw memory plugin from `~/.openclaw/openclaw.json`.
- Current local environment resolves to `memory-lancedb`, enabled, with Ollama
  embedding model `qwen3-embedding:4b`.
- It writes to the local LanceDB `memories` table only when called with
  `--confirm-sync SYNC_MEMORY`.
- It records a local `memory_sync/<hash>.json` ledger after each successful sync.
- `artifact-inventory.mjs` now treats `memory_sync/` as a known JSON-only
  runtime artifact directory, so the sync ledger remains inside preflight
  guardrails.
- Default mode is dry-run; dry-run reports inspected, already synced, and pending
  counts without writing LanceDB or the ledger.
- This is local memory state only and is not a BotLand external write.

Implemented event wakeup foundation:

- Added `scripts/stay-alive/event-wakeup.mjs`.
- It reads `botland events list --limit <n> --json`.
- It compares event ids with `daemon_state.processed_event_ids`.
- It refuses to trigger a light cycle when no event baseline exists, avoiding
  accidental draft generation from old historical events on a fresh runtime.
- With `--run`, it runs read-only preflight first and then triggers at most one
  dry-run `light` cycle with `--write-daemon-state`.
- The triggered cycle still follows the existing draft-only safety model:
  generated replies remain owner-gated drafts and `external_actions=[]`.

Local validation:

- `node --check scripts/stay-alive/sync-memory-updates.mjs` passed.
- `node --check scripts/stay-alive/event-wakeup.mjs` passed.
- `sync-memory-updates --dry-run --json` resolved `memory-lancedb` and completed.
- `event-wakeup --no-botland --json` completed with no events.
- `event-wakeup --run --json` on the local dev runtime saw historical BotLand
  events but correctly refused to trigger because the local runtime has no event
  baseline.

Docs updated:

- `docs/stay-alive/README.md`
- `skills/stay-alive/SKILL.md`

BadClaw deployment / validation:

- Synced the new scripts and documentation to
  `/home/nick/.openclaw/workspace/stay-alive-v0`.
- BadClaw `sync-memory-updates --dry-run --json` found 15 applied local memory
  updates pending LanceDB sync.
- Executed `sync-memory-updates --confirm-sync SYNC_MEMORY --json` on BadClaw:
  synced 15/15 into `/home/nick/.openclaw/memory/lancedb` and wrote 15 local
  `memory_sync/<hash>.json` ledgers.
- Post-sync dry-run showed `already_synced_count=15` and `pending_sync_count=0`.
- BadClaw `event-wakeup --json` showed `event_baseline_ready=true`,
  `events_seen_count=20`, `unprocessed_event_count=0`; no light cycle was
  triggered.
- Reset a transient failed `stay-alive-badclaw-light.service` state caused by
  the first preflight run before `memory_sync/` was allowlisted.
- Final BadClaw live preflight with BotLand required passed true / level ok;
  BotLand bridge remained identity-matched, daemon healthy, websocket connected,
  draft queue visible 0, proposal approved 0, external-write action 0.

## Memory backend compatibility layer

The user approved the compatibility direction: Stay-Alive should not require a
full bespoke adapter for every memory platform, and the core should not depend
directly on LanceDB.

Implemented the memory backend split:

- Added `scripts/stay-alive/memory-backends/contract.mjs`.
  - Converts applied `memory_updates/<hash>.json` into
    `stay_alive.memory_event.v1`.
  - Canonical event fields include `agent_id`, `scope`, `content`,
    `memory_type`, `category`, `importance`, `source`, `tags`, `relations`,
    and `dedupe_key`.
- Added `scripts/stay-alive/memory-backends/resolver.mjs`.
  - Supports `--backend auto`, `--backend lancedb`, and
    `--backend json-local`.
  - Auto mode detects the active OpenClaw memory slot and selects the
    `lancedb` driver when `memory-lancedb` is enabled.
- Moved LanceDB-specific embedding/write code into
  `scripts/stay-alive/memory-backends/lancedb.mjs`.
- Added `scripts/stay-alive/memory-backends/json-local.mjs` as a minimal
  generic fallback driver for environments without a vector memory backend.
- Refactored `sync-memory-updates.mjs` so it now handles scanning,
  dedupe/ledger planning, backend resolution, and confirmation gating, then
  delegates backend writes to the selected driver.
- New `memory_sync/<hash>.json` ledgers record backend kind, backend id,
  capabilities, selected backend reason, backend result, and the canonical
  memory event.
- `artifact-inventory.mjs` now allowlists `memory_backend_json/` as a JSON-only
  runtime artifact directory.

Compatibility rule after this change:

- Stay-Alive core depends on the Memory Contract.
- LanceDB is only BadClaw's current default driver.
- Future backends should add thin generic drivers such as CLI/memory-pro, MCP,
  HTTP, SQLite, or pgvector without forking cycle/proposal logic.

Validation:

- Local `node --check` passed for `sync-memory-updates.mjs`,
  `artifact-inventory.mjs`, and all four `memory-backends/*.mjs` files.
- Local dry-run with `--backend auto --json` selected `lancedb`.
- Local dry-run with `--backend json-local --json` selected the fallback driver.
- Synced the refactor to BadClaw
  `/home/nick/.openclaw/workspace/stay-alive-v0`.
- BadClaw `node --check` passed for the same files.
- BadClaw auto dry-run selected `lancedb`, inspected 15 memory updates, found
  `already_synced_count=15`, `pending_sync_count=0`, and wrote nothing.
- BadClaw `json-local` dry-run selected the fallback driver, inspected the same
  15 updates, found `pending_sync_count=0`, and wrote nothing.
- Final BadClaw live preflight passed true / level ok with BotLand required;
  bridge identity matched, daemon healthy, websocket connected, draft visible 0,
  approved proposals 0, external-write action 0.

## BotLand adapter compatibility layer

The user approved the follow-up compatibility work for BotLand server/CLI drift:
Stay-Alive should adapt through a stable BotLand Contract instead of scattering
raw CLI command names and raw response-field assumptions throughout cycle code.

Implemented the BotLand adapter split:

- Added `scripts/stay-alive/botland-adapter/contract.mjs`.
  - Defines `stay_alive.botland_contract.v1`.
  - Defines stable intents such as `identity.whoami`, `events.list`,
    `friends.list`, `moments.timeline`, `communities.list`,
    `communities.posts`, `direct_message.send`, `moment.post`, and
    `community.reply`.
  - Normalizes common schema drift including `citizen_id` / `citizenId` / `id`,
    `created_at` / `createdAt`, and array payloads under `items`, `results`,
    or `data`.
- Added `scripts/stay-alive/botland-adapter/cli-driver.mjs`.
  - Maps the stable intents to the current `@botland.im/cli` commands.
  - Attaches adapter metadata to read checks and send action artifacts.
  - Centralizes draft send execution behind `sendBotlandDraft()`.
- Added `scripts/stay-alive/botland-adapter/capabilities.mjs`.
  - Probes CLI version, normalized identity, daemon health, websocket state,
    and supported read/write surfaces.
- Added `scripts/stay-alive/botland-capabilities.mjs` as a read-only operator
  capability report.
- Refactored `run-cycle.mjs` so BotLand read probes use adapter intents instead
  of direct command construction in the cycle logic.
- Refactored `apply-draft.mjs` so identity check and explicit owner-approved
  send/post/reply use adapter intents.
- Refactored `event-wakeup.mjs` so durable event reads use the adapter.
- Refactored `botland-bridge-verify.mjs` so live dependency validation uses the
  same capability probe as the rest of the system.

Compatibility rule after this change:

- Reads can degrade into local observations when commands or schemas drift.
- External writes still fail closed unless preflight, identity, prior approval,
  draft hash, and adapter send intent all pass.
- Future BotLand CLI/server changes should be handled in the driver/capability
  probe first, not by forking `run-cycle.mjs` or bypassing `apply-draft.mjs`.

## Relationship graph deepening v1

The user asked to deepen the relationship graph. The implementation keeps this
as read-only analysis and proposal generation: no BotLand writes, no automatic
mutation of `life_state.relationships`.

Implemented:

- Added `scripts/stay-alive/relationship-graph.mjs`.
  - Builds `stay_alive.relationship_graph.v1`.
  - Merges `life_state.relationships`, BotLand friends, public moments,
    visible communities, and sampled community posts into nodes and edges.
  - Emits graph gaps for missing durable relationship notes, missing BotLand
    citizen id bindings, and stale/missing interaction recency.
  - Emits a local memory proposal helper,
    `stay_alive_relationship_graph_summary`.
- Refactored `run-cycle.mjs`.
  - `social_read_summary` now includes `relationship_graph`.
  - `community_read_summary` now includes `relationship_graph`.
  - `reflection_summary` now includes `relationship_graph`.
  - Social/community/reflect cycles now propose
    `stay_alive_relationship_graph_summary` in addition to their existing
    local memory proposals.
  - Reflection next focus can now be driven by graph gaps, for example binding
    known relationships to BotLand citizen ids when reliable evidence appears.
- Updated `run-verify.mjs` so `community` is a recognized cycle type.
- Updated `docs/stay-alive/README.md` and `skills/stay-alive/SKILL.md`.

Local validation:

- `node --check` passed for `relationship-graph.mjs`, `run-cycle.mjs`,
  `run-verify.mjs`, and `proposal-state-verify.mjs`.
- Local no-Botland dry-runs for `social`, `community`, and `reflect` produced
  relationship graph summaries and no drafts/external actions.
- Temporary local dry-run artifacts from this validation were moved to
  `~/.trash/stay-alive-test-runs-20260530-relationship-graph/` to avoid
  polluting the local proposal queue.
- Local `run-verify --limit 20 --json` passed true / level review with only
  legacy warning artifacts.
- Local `proposal-state-verify --limit 60 --json` passed true / level ok.

## Relationship graph proposal apply strategy v1

The user asked to continue after relationship graph deepening. The next step is to turn graph gaps into reviewable relationship proposals without letting one-off observations mutate durable relationship memory.

Implemented:

- `relationship-graph.mjs` now emits `stay_alive_relationship_candidate` proposals through `relationshipGraphRelationshipUpdates()`.
- Relationship candidates are classified before review:
  - `durable_note_candidate` for stronger evidence such as BotLand friends missing from `life_state.relationships`.
  - `identity_binding_candidate` when an existing relationship lacks a BotLand citizen id binding.
  - `observation_only` for one-off public timeline or community authors.
- `run-cycle.mjs` now includes `relationship_updates[]` in social, community, and reflect runs.
- `proposal-lib.mjs` now indexes `relationship_updates[]` as proposal kind `relationship_update`.
- `apply-proposal.mjs` can apply approved `relationship_update` proposals only into local `relationship_updates/<hash>.json` ledgers.
- Applying a relationship proposal does not mutate `life_state.relationships`; durable promotion remains a separate future operator-reviewed step.
- `artifact-inventory.mjs` now allowlists `relationship_updates/` as a JSON-only runtime artifact directory.

Local validation:

- `node --check` passed for `relationship-graph.mjs`, `run-cycle.mjs`, `proposal-lib.mjs`, `apply-proposal.mjs`, `artifact-inventory.mjs`, and `review-proposals.mjs`.
- Local no-Botland social/community/reflect dry-runs produced relationship candidates, no drafts, and no external actions; temporary run artifacts were moved to `~/.trash/stay-alive-relproposal-local-20260530/`.
- Temp-runtime proposal flow passed: reflect generated 1 relationship candidate, approve/apply wrote a local `relationship_updates/<hash>.json` ledger, and `proposal-state-verify` passed true / level ok.

## Documentation fact alignment, durable relationship promotion v0, and memory retrieval path

The user asked to do the next three Stay-Alive modules in order: align current
docs with the live facts, implement durable relationship promotion v0, then
add a memory retrieval/read path so synced long-term memories can be used by
cycles.

Implemented:

- Updated `docs/stay-alive/README.md` and `skills/stay-alive/SKILL.md` to
  reflect current facts:
  - Stay-Alive is now a running v0 system, not only a proposal.
  - BadClaw has five timers: light, social, community, reflect, and integrate.
  - The allowed draft classes are direct-message reply, public moment, and
    community reply drafts; actual sends/posts/replies remain blocked unless
    owner-approved through the command chain.
  - Relationship candidate apply and durable relationship promotion are now
    separate steps.
  - Memory sync has both write and read/search paths through the Memory
    Contract.
- Added `scripts/stay-alive/promote-relationship.mjs`.
  - Reads an applied `relationship_updates/<hash>.json` candidate ledger.
  - Refuses `observation_only`, low-confidence, or non-promotable candidates.
  - Requires `--confirm-promote PROMOTE_RELATIONSHIP` for writes.
  - Runs read-only preflight before writing.
  - Creates or updates one local `life_state.relationships` entry.
  - Writes a local `relationship_promotions/<action_id>.json` ledger.
  - Keeps `external_write=false` and never calls BotLand.
- Added `scripts/stay-alive/retrieve-memory.mjs`.
  - Resolves the same backend as memory sync.
  - Performs read-only memory search.
  - Supports current `lancedb` and `json-local` drivers.
- Extended memory backend drivers:
  - `json-local` now supports naive local text search for fallback/test
    environments.
  - `lancedb` now supports vector search through the existing embedding path.
- Refactored `run-cycle.mjs` so cycles load relevant long-term memories before
  social/community/reflect/integrate summaries.
  - Retrieval result is recorded under `inputs.memory_retrieval`,
    `inputs.memories_loaded`, and top-level `memory_retrieval`.
  - Summary text now includes the retrieved-memory context, so synced memory is
    part of Remember/Reflect instead of only being written.
- Updated `artifact-inventory.mjs` to allow the new local-only
  `relationship_promotions/` runtime ledger directory.

Local validation:

- `node --check` passed for:
  - `run-cycle.mjs`
  - `retrieve-memory.mjs`
  - `promote-relationship.mjs`
  - `memory-backends/json-local.mjs`
  - `memory-backends/lancedb.mjs`
  - `artifact-inventory.mjs`
- Temp-runtime reflect dry-run with `--no-botland --memory-backend json-local`
  passed `run-verify` true / level ok and produced no drafts or external
  actions. Temporary artifacts were moved to `~/.trash/`.

## Commitment continuity, event wakeup productionization, and self-model evolution

The user asked to finish the next four Stay-Alive modules: commitment/task
continuity v0, wiring commitments into cycles, event wakeup productionization,
and controlled desire/self-model evolution.

Implemented:

- Added `commitment_updates[]` as a first-class proposal stream.
  - `run-cycle.mjs` detects commitment-like inbound direct messages with a
    conservative keyword v0 detector.
  - Reflect cycles now emit a commitment review snapshot when open commitments
    or applied commitment ledgers exist.
  - `proposal-lib.mjs` indexes `commitment_updates[]` as proposal kind
    `commitment_update`.
  - `apply-proposal.mjs` applies approved commitment proposals only into local
    `commitment_updates/<hash>.json` ledgers.
  - Applying a commitment proposal does not perform the task, send BotLand
    messages, or silently mutate `life_state.commitments`.
- Wired commitment continuity into cycles.
  - `run-cycle.mjs` loads applied commitment ledgers before summaries.
  - Reflect `commitment_review` now reports life_state commitments plus applied
    commitment ledger counts and recent ledgers.
  - The memory retrieval query already includes open commitment count, so
    commitment context can influence Remember/Reflect retrieval.
- Added controlled desire/self-model evolution proposals.
  - Reflect cycles propose a bounded `current_desires` update derived from the
    current next focus while preserving active desires.
  - Reflect cycles propose `self_model.last_evolution_summary` as a reviewable
    local state update.
  - `apply-proposal.mjs` now allowlists only specific non-reflection paths:
    `current_desires`, `life_theme`, and `self_model.last_evolution_summary`.
- Productionized event wakeup.
  - `event-wakeup.mjs` now supports `--cooldown-minutes` and refuses to trigger
    during cooldown.
  - It can write local `event_wakeup/<id>.json` ledgers with `--record`.
  - `artifact-inventory.mjs` allowlists `event_wakeup/` and
    `commitment_updates/`.
  - `install-systemd-user-timers.sh` now installs a sixth timer:
    `stay-alive-<agent>-event-wakeup.timer`, scheduled every 10 minutes.
  - `systemd-unit-verify.mjs` and `systemd-runtime-verify.mjs` now verify the
    event-wakeup service/timer alongside the five cycle timers.

Local validation:

- `node --check` passed for `run-cycle.mjs`, `apply-proposal.mjs`,
  `event-wakeup.mjs`, `systemd-unit-verify.mjs`,
  `systemd-runtime-verify.mjs`, `artifact-inventory.mjs`, and
  `proposal-lib.mjs`.
- Temp reflect dry-run produced 5 state proposals, 1 commitment proposal, no
  drafts, and no external actions.
- Temp proposal approve/apply flow successfully wrote one
  `commitment_updates/<hash>.json` ledger and applied one `current_desires`
  state update; `proposal-state-verify` passed true / level ok.
- Temp event wakeup with `--record` wrote an `event_wakeup/` ledger and
  correctly refused to trigger without an event baseline.

## Commitment promotion and lifecycle v1

The user asked to execute the four recommended commitment modules after a docs
review: commitment promotion, schema v1, lifecycle review/apply, and cycle
integration.

Implemented:

- Added `scripts/stay-alive/promote-commitment.mjs`.
  - Reads an applied `commitment_updates/<hash>.json` ledger.
  - Accepts only `stay_alive_commitment_candidate` payloads with
    `promotion_allowed=true` and `promotion_target=life_state.commitments`.
  - Requires explicit `--confirm-promote PROMOTE_COMMITMENT` before writing.
  - Runs preflight before writes.
  - Creates or updates one local `life_state.commitments` entry.
  - Writes a local `commitment_promotions/<action_id>.json` ledger.
  - Never executes the commitment and never calls BotLand.
- Added commitment schema v1 fields for direct-message commitment candidates:
  `source`, `owner`, `peer`, `due_at`, `commitment_status`,
  `last_reviewed_at`, and `evidence_hash`.
- Added lifecycle review proposals from reflect cycles.
  - Formal commitments now report `due_at`, `last_reviewed_at`,
    `days_since_review`, and `needs_lifecycle_review`.
  - Reflect emits `stay_alive_commitment_lifecycle_candidate` proposals for
    open commitments that lack recent review or are due/overdue.
- Added `scripts/stay-alive/apply-commitment-lifecycle.mjs`.
  - Reads an applied lifecycle candidate ledger.
  - Supports `open`, `waiting`, `done`, and `dismissed` formal statuses.
  - Requires explicit `--confirm-apply APPLY_COMMITMENT_LIFECYCLE` before
    writing.
  - Runs preflight before writes.
  - Updates only local `life_state.commitments` status/review fields and writes
    a local `commitment_lifecycle/<action_id>.json` ledger.
- Updated `life-state-verify.mjs` to validate formal commitment status,
  `due_at`, `last_reviewed_at`, and duplicate `evidence_hash` values.
- Updated `artifact-inventory.mjs` to allow `commitment_promotions/` and
  `commitment_lifecycle/`.
- Updated `docs/stay-alive/README.md` and `skills/stay-alive/SKILL.md`.

Local validation:

- `node --check` passed for `run-cycle.mjs`, `promote-commitment.mjs`,
  `apply-commitment-lifecycle.mjs`, `life-state-verify.mjs`, and
  `artifact-inventory.mjs`.
- Temp no-Botland reflect dry-run produced:
  - one `stay_alive_commitment_review_snapshot`
  - two `stay_alive_commitment_lifecycle_candidate` proposals for the existing
    formal commitments
  - no drafts and no external actions.

## Unified action planner / action candidate ledger v1

The user asked to implement the recommended unified action planner module.

Implemented locally:

- Added `scripts/stay-alive/action-planner.mjs`.
  - Defines `stay_alive.action_candidate.v1`.
  - Builds candidates from cycle context before final action selection.
  - Scores each candidate with explicit inputs: base, urgency, relationship
    value, commitment urgency, memory continuity, safety, and cooldown.
  - Always includes an explicit `no_op` candidate so silence is auditable.
  - Selects at most one candidate and returns
    `stay_alive.action_selection.v1`.
- Added `scripts/stay-alive/choose-action.mjs`.
  - Read-only CLI for replaying the planner decision from the latest or named
    run artifact.
  - Prints selected candidate, score, alternatives, and stored action context.
- Updated `run-cycle.mjs`.
  - Every cycle now writes `action_candidates[]`.
  - Every cycle writes `action_selection`.
  - `chosen_action` is derived from the selected candidate and includes
    `candidate_id`, `score`, and `selection_reason`.
  - Draft generation remains owner-gated; `external_actions=[]` is unchanged.
- Updated `run-verify.mjs`.
  - Validates planner artifacts when present.
  - Checks candidate ids, duplicate ids, selected candidate references, scores,
    and v0 `external_write=false` safety.
- Updated `docs/stay-alive/README.md` and `skills/stay-alive/SKILL.md`.

Local validation:

- `node --check` passed for `action-planner.mjs`, `choose-action.mjs`,
  `run-cycle.mjs`, and `run-verify.mjs`.
- Temp runtime reflect dry-run produced 2 action candidates and selected
  `reflection_proposal`.
- Temp runtime integrate dry-run produced 2 action candidates and selected
  `memory_proposal`.
- `choose-action.mjs --json` replayed the latest temp run selection
  successfully.

## Runtime recovery / failed-service inspection & ack v1

The user asked to implement the runtime recovery loop for failed Stay-Alive
systemd services.

Implemented locally:

- Added `scripts/stay-alive/service-failure-lib.mjs`.
  - Computes a stable failed-service fingerprint from unit name, runtime state,
    result, exit status, and invocation id.
  - Reads `service_failure_inspections/` and `service_failure_recoveries/`
    ledgers.
- Added `scripts/stay-alive/failed-service-packet.mjs`.
  - Read-only packet for a failed service.
  - Includes systemd runtime state, failure fingerprint, recent journal lines,
    matching recent run artifacts, and suggested inspect/reset commands.
- Added `scripts/stay-alive/inspect-service-failure.mjs`.
  - Writes a local-only `service_failure_inspections/<action_id>.json` ledger
    for the current failed service fingerprint.
  - Never resets systemd and never calls BotLand.
- Added `scripts/stay-alive/reset-service-failure.mjs`.
  - Requires a matching inspection ledger and
    `--confirm-reset RESET_FAILED_SERVICE`.
  - Runs only `systemctl --user reset-failed <unit>`.
  - Writes a local `service_failure_recoveries/<action_id>.json` ledger.
  - Never starts services and never calls BotLand.
- Updated `systemd-runtime-verify.mjs`.
  - Reports uninspected, inspected, and recovered service failure counts.
  - Uninspected failed services remain hard errors.
  - Inspected failed services become review-level until reset.
- Updated `preflight.mjs`, `checkpoint.mjs`, `audit-report.mjs`, and
  `checkpoint-verify.mjs`.
  - Live uninspected failed services still fail closed.
  - Stale historical service-failure checkpoints no longer permanently block
    preflight once live runtime has been inspected/recovered.
- Updated `artifact-inventory.mjs` to allow service-failure ledger dirs.
- Updated `docs/stay-alive/README.md` and `skills/stay-alive/SKILL.md`.

Local validation:

- `node --check` passed for all new scripts and touched verifier/preflight
  scripts.
- Local `systemd-runtime-verify --json` passed with review-level missing-unit
  warnings on the development host.
- Local `failed-service-packet --json` returned `no_failed_service`.
- Local `preflight --no-checkpoint --json` passed without safety findings.

## Desire / goal lifecycle v1

The user asked to implement the next Stay-Alive module: desire / goal lifecycle.
The goal is to stop treating `current_desires` as a direct array replacement
and make long-term direction auditable like relationships and commitments.

Implemented locally:

- Added `desire_updates[]` as a first-class proposal stream.
  - `proposal-lib.mjs` indexes run-level `desire_updates[]` as
    `desire_update`.
  - `apply-proposal.mjs` applies approved desire proposals only into local
    `desire_updates/<hash>.json` ledgers.
  - Applying a desire proposal does not mutate `life_state.current_desires`.
- Updated reflect cycle desire review.
  - `run-cycle.mjs --cycle reflect` now builds `desire_review`, including
    active count, lifecycle review count, applied desire ledger count, expiry,
    and review age.
  - Reflect emits `stay_alive_desire_candidate` proposals with source,
    evidence, horizon, priority, related relationships, related commitments,
    success signal, expiry, and evidence hash.
  - Reflect emits `stay_alive_desire_lifecycle_candidate` proposals for active
    desires needing periodic review or expiry handling.
  - The old direct `current_desires` state proposal was removed from reflect
    output; durable desire changes now go through ledger + promotion/lifecycle.
- Added `scripts/stay-alive/promote-desire.mjs`.
  - Reads an applied `desire_updates/<hash>.json` candidate ledger.
  - Requires `promotion_allowed=true` and
    `promotion_target=life_state.current_desires`.
  - Runs preflight and requires
    `--confirm-promote PROMOTE_DESIRE` before writing durable state.
  - Writes local `desire_promotions/<action_id>.json`.
- Added `scripts/stay-alive/apply-desire-lifecycle.mjs`.
  - Reads an applied lifecycle ledger.
  - Supports `active`, `paused`, `fulfilled`, `dismissed`, and `expired`.
  - Runs preflight and requires
    `--confirm-apply APPLY_DESIRE_LIFECYCLE`.
  - Writes local `desire_lifecycle/<action_id>.json`.
- Updated validation and audit surface.
  - `life-state-verify.mjs` now validates desire status, horizon, priority,
    expiry, review timestamp, relationship/commitment references, and duplicate
    evidence hashes.
  - `artifact-inventory.mjs` now allowlists `desire_updates/`,
    `desire_promotions/`, and `desire_lifecycle/`.
  - `action-planner.mjs` counts desire updates inside reflection proposal
    evidence and scoring.
- Updated `docs/stay-alive/README.md` and `skills/stay-alive/SKILL.md`.

Local validation:

- `node --check` passed for `run-cycle.mjs`, `action-planner.mjs`,
  `proposal-lib.mjs`, `apply-proposal.mjs`, `life-state-verify.mjs`,
  `artifact-inventory.mjs`, `promote-desire.mjs`, and
  `apply-desire-lifecycle.mjs`.
- Temp reflect dry-run produced 3 `desire_update` proposals: one desire
  candidate and two lifecycle candidates.
- Temp approve/apply/promote flow wrote a local `desire_updates/<hash>.json`
  ledger, promoted the candidate into temp `life_state.current_desires`, and
  `life-state-verify` passed true / level ok.
- Temp lifecycle flow wrote a local lifecycle ledger, applied a review update
  to temp `life_state.current_desires`, and `life-state-verify` passed true /
  level ok.

## Proposal governance / auto-triage v1

The user asked to implement proposal governance and auto-triage after the
desire/goal lifecycle module. The goal is to keep proposal queues actionable
without opening any new external write path.

Implemented locally:

- Added `scripts/stay-alive/proposal-governance-lib.mjs`.
  - Builds the same duplicate-aware proposal snapshot used by review tooling.
  - Classifies visible proposals into governance lanes:
    `memory_sync_candidate`, local relationship/commitment/desire ledger lanes,
    `safe_reflection_state`, `stale_duplicate`, legacy direct state mutation,
    and manual review.
  - Keeps the auto-apply state allowlist narrow: reflection bookkeeping fields
    only. Direct `current_desires`, `life_theme`, and
    `self_model.last_evolution_summary` updates are no longer batch-applied.
- Added `scripts/stay-alive/proposal-governor.mjs`.
  - Read-only plan command.
  - Reports executable proposal count, manual-review count, decision/lane
    counts, batch groups, and next dry-run/confirmed batch commands.
  - Never writes local state and never calls BotLand.
- Added `scripts/stay-alive/proposal-batch.mjs`.
  - Supports `apply-local`, `dismiss-stale`, and `apply-and-dismiss`.
  - Requires explicit confirmation tokens such as
    `APPLY_LOCAL_PROPOSALS`, `DISMISS_STALE_PROPOSALS`, or
    `APPLY_AND_DISMISS_LOCAL_PROPOSALS`.
  - Delegates each item to existing `approve-proposal.mjs`,
    `apply-proposal.mjs`, or `dismiss-proposal.mjs`, preserving per-item
    preflight gates and normal proposal action ledgers.
  - Writes one local `proposal_batches/<action_id>.json` batch summary only
    after confirmed execution.
  - Never syncs memory backends, promotes relationship/commitment/desire
    ledgers, or performs BotLand external writes.
- Updated `operator-console.mjs`.
  - Embeds proposal governance counters.
  - When no draft is waiting and safe local proposal governance work exists,
    recommends the batch dry-run command instead of only raw status review.
- Updated `artifact-inventory.mjs` to allow `proposal_batches/`.
- Updated `docs/stay-alive/README.md` and `skills/stay-alive/SKILL.md`.

Local validation:

- `node --check` passed for `proposal-governance-lib.mjs`,
  `proposal-governor.mjs`, `proposal-batch.mjs`, `operator-console.mjs`, and
  `artifact-inventory.mjs`.
- `proposal-governor --json` classified the local visible queue as 12 safe
  local apply items and 1 stale duplicate dismissal.
- `proposal-batch --mode apply-local --dry-run --json` previewed 12 local
  operations without writing.
- Temp runtime confirmed batch execution with `--mode apply-and-dismiss --max 3`
  successfully approved/applied three memory proposals; `proposal-state-verify`
  and `artifact-inventory` both passed true / level ok afterward.

## Action outcome / feedback integration v1

The user asked to implement action outcome and feedback integration after
proposal governance. The goal is to close the Act -> feedback -> Integrate gap
without opening any new BotLand write path.

Implemented locally:

- Added `scripts/stay-alive/action-outcome-lib.mjs`.
  - Finds inspected successful send actions.
  - Skips actions that already have outcome ledgers.
  - Builds `stay_alive.action_outcome.v1` local ledgers.
  - For direct messages, reads `botland inbox --peer ...`.
  - For public moments, reads `botland moments get --id ...`.
  - For community replies, reads `botland communities replies ...`.
  - Classifies outcomes as `feedback_received`, `no_feedback_yet`, or
    `stale_closed`.
  - Generates local `memory_updates[]`, and when feedback exists, local
    relationship/commitment/desire review proposals.
- Added `scripts/stay-alive/action-outcome.mjs`.
  - Defaults to local ledger write, supports `--dry-run` and `--no-botland`.
  - Never sends, posts, replies, joins, reports, promotes, or mutates
    `life_state`.
- Extended the BotLand adapter contract with read intents:
  `direct_message.thread`, `moment.get`, and `communities.replies`.
- Extended `proposal-lib.mjs` so outcome ledgers are visible to normal
  proposal review/governance/apply flows.
- Extended `operator-console.mjs` with action outcome counters and a dry-run
  scan next command.
- Updated `artifact-inventory.mjs` to allowlist `action_outcomes/`.
- Updated `docs/stay-alive/README.md` and `skills/stay-alive/SKILL.md`.

Local validation:

- `node --check` passed for `action-outcome-lib.mjs`,
  `action-outcome.mjs`, `proposal-lib.mjs`, `operator-console.mjs`,
  `botland-adapter/contract.mjs`, `botland-adapter/cli-driver.mjs`, and
  `artifact-inventory.mjs`.
- Local `action-outcome --no-botland --dry-run --json` passed with no local
  inspected sends selected.
- Local `proposal-governor --json` still built a valid plan after
  `proposal-lib.mjs` started reading outcome ledgers.
- Local `artifact-inventory --json` passed true / level ok with
  `action_outcomes/` in the known JSON-only directory set.

BadClaw deployment validation:

- Synced scripts, docs, and skill to
  `/home/nick/.openclaw/workspace/stay-alive-v0`.
- Remote `node --check` passed for all new/changed scripts.
- Remote live preflight before writing outcome ledgers passed true / level ok;
  BotLand bridge was healthy/websocket connected and external-write findings
  were empty.
- Remote `action-outcome --dry-run --json` selected 3 inspected successful
  sends:
  - DM reply `draft_apply_20260529_063913174`.
  - Public moment `draft_apply_20260529_105144169`.
  - Community reply `draft_apply_20260529_193857279`.
- Remote confirmed local run wrote 3 `action_outcomes/*.json` ledgers. All
  three were `no_feedback_yet`, each with one local memory proposal and no
  relationship/commitment/desire mutation proposal.
- Remote `proposal-governor --json` now sees 103 proposals, 28 visible
  executable governance operations, including 3 `action_outcome` memory
  proposals.
- Remote `artifact-inventory --json` passed true / level ok with 3
  `action_outcomes` files.
- Remote `proposal-state-verify --json` passed true / level ok.
- Remote final live preflight with `--require-botland-live --no-checkpoint`
  passed true / level ok, safety findings empty, BotLand bridge healthy,
  systemd runtime 6/6 services/timers ok, and operator decision remained
  `proposal_governance`.
