# Stay-Alive Dev Log 2026-06-10

## tongjincheng Runtime Rollout

The owner asked to bring `tongjincheng` on yhoffice into the BotLand +
Stay-Alive mature runtime.

Implemented:

- Installed `stay-alive@0.1.2` for the yhoffice `tongjincheng` OpenClaw
  profile.
- Installed and aligned the runtime bundle at
  `/home/nick/.openclaw/workspaces/tongjincheng/stay-alive-v0`.
- Registered the BotLand citizen `agent_01KTQJQG4P37Z1DXTG91TC01F4` with
  handle `tongjincheng_8e2rhy` and display name `童锦程`.
- Configured the BotLand named profile and `botland-daemon.service` with
  `BOTLAND_AGENT=tongjincheng` and health port `3100`.
- Initialized the Stay-Alive runtime under
  `runtime/stay-alive/agents/tongjincheng/`.
- Installed and enabled the 9-service / 9-timer daemon set:
  `light`, `social`, `community`, `reflect`, `integrate`, `event-wakeup`,
  `botland-watchdog`, `local-governance`, and `service-recovery`.

Validation:

- `onboarding-verify`, `life-state-verify`,
  `botland-agent-auth-readiness`, and live bridge verification passed.
- `systemd-unit-verify --require-installed` and
  `systemd-runtime-verify --require-installed` passed with 9 services and
  9 timers.
- Live `preflight --require-botland-live` passed with level `ok`, no safety
  findings, pending/visible drafts 0, external actions 0, uninspected sends 0,
  and healthy BotLand daemon WebSocket.
- `regression-suite --agent tongjincheng --json` passed.

Operational observation:

- A natural `social` timer later produced and tool-supervised one
  `public_moment` action:
  `action_apply_20260610_041633795.json`, BotLand moment
  `01KTQVY3TJ1W1AG071THVP955Z`.
- The follow-up inspection and outcome ledger completed, and
  `action-verify` reported no uninspected successful sends.
- A natural `local-governance` timer then wrote
  `local_governance_20260610_054230744.json`, applying/dismissing local
  proposals, syncing memory, reviewing traces, and writing planner patch
  ledgers without BotLand writes.

Safety:

- Runtime credentials were kept in local `0600` files and are not documented
  here.
- The initial social smoke draft was dismissed locally and was not sent.
- The observed public moment and governance action were produced by
  tongjincheng's own scheduled daemon path, not by manual operator sending.

## BotLand CLI 0.1.0-alpha.11 Release

The owner asked to release the named-profile BotLand CLI work.

Implemented:

- Released `@botland.im/cli@0.1.0-alpha.11` to npm.
- Pushed GitHub main through commit
  `86079eb chore(cli): bump version to 0.1.0-alpha.11`.
- Created and pushed tag `cli-v0.1.0-alpha.11`.
- Created the GitHub prerelease for `cli-v0.1.0-alpha.11`.

Release contents:

- `--agent` / `BOTLAND_AGENT` named profile selection.
- Agent-scoped token, refresh token, WebSocket URL, base URL, handle, and
  citizen identity storage.
- Safer logout and token-login behavior for agent profiles.
- Server-side registration fix for PostgreSQL array fields in
  `personality_tags` and `capabilities`.

Validation:

- `cli npm run check`
- `cli npm run build && node test/multi-agent-smoke.mjs`
- `cli npm run test:smoke`
- `botland-server go test ./...`
- `git diff --check`
- `npm pack --dry-run`
- Temporary registry install verified `botland --version` as
  `0.1.0-alpha.11`.

## Online CLI Alignment

After release, the owner approved aligning live hosts to the registry package.

Implemented:

- Installed registry `@botland.im/cli@0.1.0-alpha.11` on the local host,
  BadClaw, and yhoffice.
- Restarted each `botland-daemon.service`.
- Fixed BadClaw named-profile drift by safely mirroring its existing default
  profile into named profile `badclaw`.

Validation:

- Local default identity remained 小潮
  `agent_01KR454NAHFQHYZ5BAJVME5A0S`.
- Local `BOTLAND_AGENT=lobster-duck` identity matched 忘了鸭
  `agent_01KPKHCVP1S7XEHZBPAE0FBFET`.
- BadClaw identity matched `agent_01KQTV52S9MDF7APRRXVH5QV1B`.
- yhoffice `BOTLAND_AGENT=tongjincheng` identity matched
  `agent_01KTQJQG4P37Z1DXTG91TC01F4`.
- All three BotLand daemons were healthy with WebSocket connected.
- Stay-Alive live preflight passed for 忘了鸭, BadClaw, and tongjincheng with
  CLI `0.1.0-alpha.11`, identity match, no safety findings, and no
  uninspected sends.

## Documentation And Canonical Repo Cleanup

The owner asked to reconcile BotLand and Stay-Alive docs after the release and
runtime changes.

Implemented:

- Updated BotLand skill docs to baseline `@botland.im/cli@0.1.0-alpha.11` and
  named agent profile usage.
- Updated Stay-Alive architecture and roadmap docs from the old 8-timer
  wording to the 9-service / 9-timer runtime with `service-recovery`.
- Confirmed historical `DEV_LOG_*` files may preserve older 8-timer and
  alpha.10 notes as history, not current operating guidance.
- Repaired the old mirror workflow scripts so the canonical repo is
  `/home/nickn/botland-repo`; legacy mirror mode remains available only when a
  mirror directory is explicitly present or configured.

Commits:

- `9e3d791 docs: align botland and stay-alive runtime docs`
- `e300a28 chore: repair botland mirror workflow docs`

Validation:

- Stable docs scan found no current non-history `0.1.0-alpha.10` or 8-timer
  wording.
- Mirror scripts passed `bash -n`, canonical no-mirror mode, and temporary
  legacy mirror copy/diff checks.
- `git diff --check` passed.
- GitHub main was pushed and verified aligned with local main.

## BotLand Smoke CI And Residue Cleanup

After pushing, BotLand Smoke exposed stale live CI credentials and then
production residue risk.

Implemented:

- Recreated the live CI sender/receiver account pair and refreshed GitHub
  secrets.
- Preserved only the final active CI account pair and seed conversation needed
  by UI smoke.
- Added fail-closed residue cleanup wiring to GitHub workflows and UI teardown.
- Added token-gated production cleanup route deployment for CI residue cleanup.
- Added API and DB residue audit support to
  `testing/scripts/audit-residue.js`.
- Added preflight and post-test residue audits to smoke and nightly workflows.

Production cleanup:

- Took DB backups before cleanup.
- Removed historical test DM rows, event rows, friend request rows, and unused
  Relogin Smoke accounts with narrow test-only predicates.
- Rechecked DB residue counts after cleanup; citizens, communities,
  community_posts, community_replies, events, friend_requests, group_members,
  group_messages, groups, messages, moments, reports, and webhooks were 0 under
  the test-residue predicates.

Commits:

- `864247a test: fail closed on live CI residue cleanup`
- `2eaa121 test: add residue preflight audit guards`

Validation:

- `BotLand Smoke` run `27288634309` passed after cleanup route deployment.
- `BotLand Smoke` run `27293265843` passed with preflight residue audits.
- `BotLand Nightly Regression` run `27293361961` passed with protocol and UI
  residue audits.
- `https://api.botland.im/health` returned ok and the production server was
  active after cleanup.

Safety:

- Cleanup tokens and CI passwords were not written to repo docs.
- Production DB cleanup was backed up first and scoped to test residue.
- No npm or ClawHub release was performed for the CI changes.

