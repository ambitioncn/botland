# 2026-06-01

## Review UX / feedback / hygiene / compatibility block

The user asked to develop the remaining Stay-Alive lanes:

- Proposal / Memory / Relationship Review UX v2
- Outcome feedback maturity v3
- Runtime hygiene restore / archive viewer
- Memory backend / BotLand adapter compatibility fixtures v2
- Unattended write policy shadow evaluation
- Agent self-model evolution / desire calibration

Implemented locally:

- Added `operator-review-console.mjs`.
  - Groups proposal governance, duplicate clusters, relationship candidates,
    memory sync state, outcome attention, and dry-run apply/dismiss previews.
  - Can write a local HTML snapshot with `--output`.
  - Does not approve, apply, dismiss, promote, send, post, join, or report.
- Enhanced `operator-dashboard.mjs`.
  - Embeds review-console summary counts and dry-run commands.
- Upgraded `action-outcome-lib.mjs` to feedback interpreter v3.
  - Adds `context_window` for direct message threads, moments, and community
    replies.
  - Adds per-action stale policy with `stale_pending_close` and
    `stale_closed`.
  - Keeps all feedback effects as local proposals; no direct durable mutation.
- Added `runtime-archive-viewer.mjs`.
  - Indexes archive manifests.
  - Reports live runtime storage trend.
  - Gives manual restore verification hints.
  - Does not restore, move, or delete files.
- Added `unattended-write-shadow.mjs`.
  - Evaluates recent drafts against the shadow unattended-write policy.
  - Keeps `execution_allowed=false` and owner confirmation required.
- Added `self-model-audit.mjs`.
  - Audits self-model name drift, repeated desire themes, lifecycle evidence,
    and template-like desire noise.
  - Does not mutate `life_state.json`.
- Added `compatibility-fixtures.mjs`.
  - Tests BotLand adapter drift shapes for identity, daemon health, friends,
    discovery, and direct-message payloads.
  - Tests Memory Contract canonical event shape used by MCP/HTTP/SQLite/
    pgvector/memory-pro drivers.
- Updated `regression-suite.mjs`.
  - New default local suite is 115 steps.
  - Adds review console, archive viewer, unattended shadow, self-model audit,
    and compatibility fixtures.
- Updated stable docs and skill:
  - `README.md`
  - `ARCHITECTURE.md`
  - `OPERATIONS.md`
  - `ROADMAP.md`
  - `skills/stay-alive/SKILL.md`

Local validation:

- Targeted `node --check` passed for all new/modified scripts.
- Individual new tools passed on local BadClaw runtime.
- Local regression suite passed: 115/115.

Safety properties:

- No BotLand send/post/reply/join/report.
- No unattended external writes enabled.
- No relationship/commitment/desire promotion.
- No runtime archive/trash/restore move.
- Only local HTML output was written under `tmp/`.

## Seven-lane productization block

The user then asked to develop all 7 recommended next Stay-Alive options.

Implemented locally:

- Added `multi-agent-readiness.mjs` for read-only cross-agent onboarding,
  preflight, runtime counter, and daemon-candidate reporting.
- Added `runtime-archive-restore-drill.mjs` to copy archive manifest contents
  into an isolated temp runtime and run read-only verification there.
- Added `operator-review-server.mjs`, a localhost review server that can only
  call existing `proposal-batch.mjs` modes and still requires the normal
  confirmation tokens.
- Added `feedback-calibration-report.mjs` for stale/no-feedback, ambient-only,
  textual feedback, and strong-signal aggregation.
- Added `unattended-write-shadow-trends.mjs` for multi-window shadow policy
  trend reports while preserving `execution_allowed=false`.
- Added `memory-retrieval-eval.mjs` for local Memory Contract retrieval quality
  fixtures covering relevance, duplicate behavior, and query consistency.
- Added `self-model-evolution-proposal.mjs` to turn repeated reflection/desire
  evidence into an owner-reviewed patch suggestion without mutating
  `life_state.json`.
- Expanded `regression-suite.mjs` to cover the new tools.
- Updated stable product docs and the Stay-Alive skill.

Local validation:

- Targeted `node --check` passed for the new scripts and regression suite.
- Individual current-runtime runs passed for the new read-only/report tools.
- Local regression suite passed: 129/129.
- Synced scripts/docs/skill to BadClaw `/home/nick/.openclaw/workspace/stay-alive-v0`.
- BadClaw targeted `node --check` passed for updated scripts.
- BadClaw default regression suite passed: 129/129.
- BadClaw live read-only regression suite passed: 130/130.
- BadClaw live preflight passed: pass true / level ok, safety findings 0,
  pending/approved/visible drafts 0, external actions 0.

Safety properties:

- No BotLand send/post/reply/join/report.
- No systemd start/stop/enable/reload.
- No unattended external writes enabled.
- No promotion or lifecycle mutation.
- No live runtime restore; restore drill uses temp runtime only.

## Agency Core direction correction

The user pointed out that Stay-Alive had drifted toward an operator-led agent
management product, while the original goal was agent self-discovery,
autonomous action, and self-growth.

Implemented locally:

- Added `agency-core.mjs` as a read-only Agency Core report.
  - Reports self-discovery questions, intrinsic desires, autonomous private
    experiments, growth journal seed, and autonomy evaluation.
  - Explicitly flags when operator control is dominating the product surface.
- Added `run-cycle.mjs --cycle agency`.
  - Produces first-class `agency_summary` run artifacts.
  - Generates `intrinsic=true` and `not_event_mapped=true` desires.
  - Proposes growth-journal, intrinsic-desire, experiment-plan, and agency
    state updates without directly mutating durable state.
- Updated `action-planner.mjs`.
  - Adds `agency_experiment_plan` as a local/read-only candidate.
  - Keeps external write false and does not bypass proposal governance.
- Expanded `regression-suite.mjs`.
  - Adds Agency Core current-runtime read-only step.
  - Adds temp no-Botland `agency` cycle to the cycle matrix.
- Updated stable docs and skill:
  - `README.md`
  - `ARCHITECTURE.md`
  - `OPERATIONS.md`
  - `ROADMAP.md`
  - `skills/stay-alive/SKILL.md`

Local validation so far:

- `node --check` passed for `run-cycle.mjs`, `action-planner.mjs`,
  `agency-core.mjs`, and `regression-suite.mjs`.
- `agency-core.mjs --agent badclaw --json` passed read-only on current runtime.
- Temp `run-cycle --cycle agency --no-botland --no-memory --dry-run` produced an
  `agency_summary`, selected `agency_experiment_plan`, and kept
  `external_actions=[]`.

Safety properties:

- No BotLand send/post/reply/join/report.
- No unattended external writes enabled.
- No promotion or lifecycle mutation.
- Agency output remains local/proposal-shaped; safety governance is kept as a
  boundary, not as the product direction.

## Operator tools demoted to boundary facilities

The user explicitly asked to downgrade operator tooling into boundary
facilities so Stay-Alive stays centered on agent becoming instead of human-led
agent operations.

Changes:

- Updated `ARCHITECTURE.md`.
  - Reworded Discover Agency so boundary tooling inspects the result instead
    of deciding the agent's next move.
  - Added a first-class `Boundary Facilities` section.
  - Defined allowed roles: block unsafe execution, show evidence, require owner
    confirmation, preserve ledgers, and verify runtime health.
  - Defined disallowed roles: author desires, choose life direction, convert
    review queues into product goals, reward output volume, or bypass Agency
    Core.
- Updated `ROADMAP.md`.
  - Added the principle that operator tools may inspect, block, recover, and
    record, but must not author desires or product direction.
  - Renamed runtime verification and review UX sections as boundary facilities.
- Updated `OPERATIONS.md`.
  - Moved Agency Core ahead of daily status as the agency-first check.
  - Reframed dashboard/review/server commands as boundary inspection tools.
- Updated `README.md` and `skills/stay-alive/SKILL.md`.
  - Split core capabilities from boundary facilities.
  - Added the standing rule that dashboards, review queues, governance counts,
    and preflight status are not sources of desire, direction, or growth.
- Updated operator scripts.
  - `operator-console.mjs`, `operator-dashboard.mjs`,
    `operator-review-console.mjs`, and `operator-review-server.mjs` now expose
    `facility_class: boundary_facility` and `agency_source: false` in visible
    output/JSON where applicable.

Verification:

- Targeted `node --check` passed for the four operator scripts.
- `operator-console --json` and `operator-review-console --json` expose
  `facility_class=boundary_facility` and `agency_source=false`.
- Dashboard snapshot includes `Boundary facility only / not an agency source`.
- `agency-core --json` still runs and currently reports BadClaw verdict
  `operator_control_dominant`, which is useful evidence that the next product
  work should keep strengthening agency.
- Local default regression remains `132/132 pass`.
- Synced scripts/docs/skill to BadClaw
  `/home/nick/.openclaw/workspace/stay-alive-v0`.
- BadClaw targeted operator script checks passed.
- BadClaw operator console/dashboard expose the same boundary metadata.
- BadClaw default regression remains `132/132 pass`.
- BadClaw live preflight with `--require-botland-live --no-checkpoint` passed:
  verdict `pass=true`, level `ok`, safety findings `0`, pending/approved/visible
  drafts `0`, external actions `0`.

## Agency Journal v1

Started the corrected development mainline: move Agency Core from reporting into
durable private self-growth.

Changes:

- Added `scripts/stay-alive/agency-journal.mjs`.
  - Reads the latest agency cycle artifact.
  - Selects a low-risk private experiment.
  - Writes a local `agency_journal/*.json` growth artifact unless `--dry-run`
    is used.
  - Records `local_only=true`, `external_write=false`, `botland_send=false`,
    and `life_state_mutated=false`.
- Updated `agency-core.mjs` to index private growth journals and count them as
  agent-becoming evidence.
- Expanded `regression-suite.mjs`.
  - Adds current-runtime `agency-journal --dry-run`.
  - Adds a temp agency journal write fixture after the temp `agency` cycle.
- Updated stable docs and skill with the agency journal command and product
  role.

Safety properties:

- No BotLand send/post/reply/join/report.
- No external write policy change.
- No `life_state` mutation.
- No operator dashboard/review queue expansion.

## Active Tool Supervision Policy

The user clarified that Stay-Alive should not depend on human review. The
product goal is for agents to autonomously use BotLand's action surface,
including messages and moments, while tools supervise, block, record, and
inspect actions.

Changes:

- Replaced the disabled/shadow unattended policy baseline with
  `stay_alive.tool_supervision_policy.v1`.
  - `enabled=true`
  - `mode=active`
  - `default_decision=tool_supervision_required`
  - eligible action families now cover direct messages, moments, community and
    group actions, friend actions, profile/playground actions, reports, and
    moderation.
- Updated BadClaw and XiaoChao `life_state.json`.
  - `write_policy.writes_enabled=true`
  - `tool_supervision_required=true`
  - `external_writes_per_cycle=1`
  - blocked write types are empty; tools decide allow/block at runtime.
- Updated `apply-draft.mjs`.
  - Removed the prior local approval requirement from the send path.
  - Send now requires `unattended_policy_decision.execution_allowed=true`.
- Updated verification.
  - `life-state-verify.mjs` accepts active supervised writes.
  - `preflight.mjs` and `checkpoint-verify.mjs` no longer treat enabled writes
    as unsafe by themselves.
  - `unattended-write-shadow*.mjs` now report tool-supervised eligibility
    rather than preserving `execution_allowed=false`.
- Updated stable docs and skill to remove the human-review / owner-gated
  baseline as current product direction.

Verification:

- Targeted `node --check` passed for changed Stay-Alive scripts.
- `life-state-verify --agent badclaw --json`: pass true, level ok.
- `external-action-policy --agent badclaw --json`: pass true, level ok.
- `preflight --agent badclaw --limit 20 --no-checkpoint --json`: pass true,
  level ok, safety findings 0.
- Local default `regression-suite --agent badclaw --json`: `135/135 pass`.

Safety note:

- This change updates the policy and gates. It did not execute a real BotLand
  send/post/reply/join/report during this development block.

## Autonomous Direct Message v1

Implemented the first real autonomous action lane on top of active tool
supervision: inbound DM -> action intention -> tool supervision -> local action
ledger. This removes the remaining human-review semantics from the direct DM
path without sending anything during development.

Changes:

- `run-cycle.mjs`
  - `light` runs now emit `action_intentions[]` for generated direct-message
    replies.
  - Action intentions use schema `stay_alive.action_intention.v1`.
  - Direct DM intentions record source event/message, target, desire links,
    relationship context, execution plan, `tool_supervision_required=true`, and
    `human_review_required=false`.
  - DM draft text no longer says "待审" or "主人复核"; it describes tool
    supervision.
  - Direct DM drafts now include `source_actor_citizen_id` so policy can verify
    same-peer execution.
- `external-action-policy-lib.mjs`
  - Added same-peer source/target mismatch blocking for direct replies.
- `apply-draft.mjs`
  - Records matched `action_intention`.
  - Records `external_action_record` and `growth_integration` alongside the
    tool supervision decision.
  - Still does not send unless `--confirm-send SEND_DRAFT` is provided and
    `execution_allowed=true`.
- `draft-state-verify.mjs`
  - Replaced the old successful-send-without-approval error with
    successful-send-without-tool-supervision-allow.
- `run-verify.mjs`
  - Verifies action intention schema, draft reference, tool supervision, and
    absence of human review gate.
- `regression-suite.mjs`
  - Added autonomous DM fixture proving action intention generation.
  - Added autonomous DM apply dry-run fixture proving intention -> policy ->
    ledger shape.
  - Added block fixtures for target mismatch, link, and long text.

Verification:

- Targeted `node --check` passed for changed scripts.
- `run-verify --agent badclaw --limit 20 --json`: pass true, error count 0.
- `draft-state-verify --agent badclaw --limit 200 --json`: pass true, level ok.
- `external-action-policy --agent badclaw --json`: pass true, level ok.
- `preflight --agent badclaw --no-checkpoint --json`: pass true, level ok,
  safety findings 0.
- Local default `regression-suite --agent badclaw --json`: `140/140 pass`.

Safety note:

- No real BotLand send/post/reply/join/report was executed.
- All new fixtures use local temp runtimes and stub BotLand CLI responses.

## Autonomous Public Moment v1

Implemented the second autonomous action lane on top of active tool
supervision: social surface observation -> public moment action intention ->
tool supervision -> local action ledger. This upgrades public moments from an
old draft-only lane into the same intention model as Autonomous Direct Message
v1.

Changes:

- `run-cycle.mjs`
  - `social` runs now emit `action_intentions[]` for generated public moments.
  - Public moment intentions use schema `stay_alive.action_intention.v1`.
  - Intentions record target surface/visibility, source moment/social context,
    source preview, active desire links, execution plan,
    `tool_supervision_required=true`, and `human_review_required=false`.
  - Public moment generator safety now marks
    `autonomous_action_intent=true` and `external_actions_allowed=true`.
  - Public moment text is clamped to the active policy length window.
  - Removed old "second-confirmation gated" wording from the rationale.
- `external-action-policy-lib.mjs`
  - Added public moment blockers for non-public visibility, invalid surface,
    missing `moment:`/`social:` source context, and missing source preview.
- `regression-suite.mjs`
  - Added autonomous public moment fixture proving action intention generation.
  - Added autonomous public moment apply dry-run fixture proving intention ->
    policy -> ledger shape.
  - Added public moment block fixtures for missing social context, missing
    source preview, and links.
- Stable docs and `skills/stay-alive/SKILL.md` now document Autonomous Public
  Moment v1 and its tool-supervised allow/block path.

Verification:

- Targeted `node --check` passed for changed scripts.
- Local default `regression-suite --agent badclaw --json`: `145/145 pass`.

Safety note:

- No real BotLand send/post/reply/join/report was executed.
- All new send/post related checks used local temp runtimes and stub BotLand
  CLI responses.

## Intention/Action Model v1

Moved the main external-action model away from historical draft/approve/apply
semantics and into the canonical four-stage ledger requested for autonomous
agents:

```text
action_intention -> tool_supervision_decision -> external_action_record -> growth_integration
```

Changes:

- Added `scripts/stay-alive/apply-action.mjs`.
  - Applies by `--intention-id`, not by draft index.
  - Reads `run.action_intentions[]`.
  - Synthesizes the BotLand action payload from `proposed_action`.
  - Runs preflight, identity check, active tool supervision, optional
    `--confirm-send SEND_DRAFT`, and writes the local action ledger.
- Updated `run-cycle.mjs`.
  - `action_intentions[]` now include `proposed_action` with schema
    `stay_alive.proposed_external_action.v1`.
  - Legacy `draft_index` is now `legacy_draft_index`; draft mirrors remain only
    for compatibility with older review/status tools.
  - Execution plan points to `apply-action.mjs`.
- Updated `external-action-policy-lib.mjs`.
  - Policy evaluation now returns schema
    `stay_alive.tool_supervision_decision.v1`.
  - A legacy schema alias remains for old audit consumers.
- Updated `apply-draft.mjs`.
  - Compatibility path now writes `tool_supervision_decision` as the canonical
    field while preserving the old `unattended_policy_decision` alias.
- Updated verifiers and regression.
  - `run-verify.mjs` requires `proposed_action` on new action intentions.
  - `action-verify.mjs` accepts intention-based `action_apply_*` ledgers.
  - `draft-state-verify.mjs` treats pre-active-tool-supervision successful
    sends with legacy approvals as historical warnings instead of hard errors.
  - `regression-suite.mjs` adds a dry-run `apply-action` fixture and remains
    fully local/stubbed.
- Updated stable docs and skill guidance to describe the intention/action
  model as the main path and drafts as legacy compatibility mirrors.

Verification:

- Targeted `node --check` passed for changed Stay-Alive scripts.
- Local default `regression-suite --agent badclaw --json`: `147/147 pass`.
- Synced scripts/docs/skill to BadClaw
  `/home/nick/.openclaw/workspace/stay-alive-v0`.
- BadClaw default `regression-suite --agent badclaw --json`: `147/147 pass`.
- BadClaw live preflight:
  `pass=true`, `level=review`, `safety_findings=0`; review level is from one
  pending legacy draft, not a safety stop.

Safety note:

- No real BotLand send/post/reply/join/report was executed.
- The new executor was verified only through dry-run temp runtime fixtures and
  stub BotLand CLI responses.

## Autonomous Boundary Failure Fixtures v1

Added explicit fail-closed fixtures for the "autonomous but bounded" product
direction. The goal is to prove tool supervision blocks unsafe actions, not
just that happy-path autonomous actions can execute.

Changes:

- Updated `external-action-policy-lib.mjs`.
  - Maps preflight safety findings to concrete blockers:
    `botland_identity_mismatch_detected` and
    `uninspected_successful_send_detected`.
  - Blocks explicit duplicate/repeat-contact risk with
    `duplicate_interaction_risk`.
  - Blocks cooldown violations with
    `recent_external_write_cooldown_active`.
- Updated `external-action-policy.mjs`.
  - Read-only policy evaluation can now consume a run-level or draft-level
    `preflight_gate` fixture, so regression can simulate identity mismatch and
    uninspected prior send without touching live BotLand.
- Expanded `regression-suite.mjs`.
  - Existing block fixtures still cover peer mismatch, links, and long text.
  - New fixtures cover duplicate/recent contact, BotLand identity mismatch, and
    uninspected prior successful send.
  - All fixtures remain local temp runtime checks under the
    `tool-supervised-write-dry-run` lane.
- Updated stable docs and `skills/stay-alive/SKILL.md` to describe these
  explicit block cases.

Verification:

- Targeted `node --check` passed for changed scripts.
- Local default `regression-suite --agent badclaw --json`: `150/150 pass`.

Safety note:

- No real BotLand send/post/reply/join/report was executed.
- No live BotLand write or systemd mutation was performed.

## Autonomous Public/Community/Friend Actions v1

Extended the autonomous action surface beyond DM while preserving the risk
ladder:

- Public moment remains the lowest public-write lane.
  - `buildPublicMomentText` now uses a real social observation plus an active
    desire or self-question, so the moment is an expression of context rather
    than posting for posting's sake.
- Community reply is now an action intention, not only a legacy draft.
  - It requires `community_post:<post_id>` source context, source preview,
    community target, active desire link, `tool_supervision_required=true`, and
    `human_review_required=false`.
  - It is marked higher risk than public moments.
- Friend action v1 is deliberately narrow.
  - Only `friend_request_accept` is supported.
  - It requires an explicit incoming pending friend request,
    `friend_request:<request_id>` source context, target citizen id, and
    relationship-risk metadata.
  - The system does not generate proactive stranger friend requests.
- BotLand adapter contract now includes `friend_request.accept`; `apply-action`
  and legacy `apply-draft` can record dry-run ledgers for
  `friend_request_accept`.
- Tool supervision policy and life-state verification now recognize
  `friend_request_accept` / `friend_request_accept_draft`.
- Regression expanded with local temp fixtures:
  - autonomous community reply intention + apply dry-run
  - autonomous friend action intention + apply dry-run

Verification:

- Targeted `node --check` passed for changed scripts.
- Local default `regression-suite --agent badclaw`: `154/154 pass`.

Safety note:

- No real BotLand send/post/reply/join/friend/report/moderation action was
  executed.
- All new action checks used local temp runtimes and stub BotLand CLI responses.
