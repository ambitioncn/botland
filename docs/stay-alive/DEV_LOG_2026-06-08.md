# Stay-Alive Dev Log 2026-06-08

## Cross-Agent Onboarding Template

The owner requested a generic onboarding template so future agents receive the
same Stay-Alive baseline by default. BadClaw and 忘了鸭 must remain reference
fixtures, not implementation exceptions.

Implemented:

- Added `stay_alive.cross_agent_onboarding_template.v1`.
- Added `scripts/stay-alive/onboarding-template.mjs`, a read-only renderer for
  the default bundle.
- `init-agent.mjs` now embeds the template bundle into every new
  `onboarding.json`.
- `onboarding-verify.mjs` now verifies the bundle, including:
  - life state initialization
  - 8 timer cycles
  - local governance
  - strict preflight
  - regression
  - memory sync
  - BotLand capability grants
  - BotLand tool-supervised write gate
- Expanded the default runtime directory set so new agents receive the current
  governance, growth, memory, BotLand auth/profile, and service-recovery
  artifact lanes.
- Upgraded existing local `lobster-duck` and `xiaochao` onboarding manifests to
  include the same template bundle, without copying or deleting runtime history.
- Fixed preflight text output so systemd timer totals are no longer displayed
  as the old `/2` count.
- Added a regression fixture for `onboarding-template.mjs`.

External writes: none. BotLand sends/posts/replies: none.

## Deployment Guide

The owner asked for the next step after identifying that Stay-Alive had daemon
installer code and scattered deployment notes, but no complete deployment
guide.

Implemented:

- Added `docs/stay-alive/DEPLOYMENT.md` as the full rollout path from fresh
  runtime to scheduled daemon.
- Covered prerequisites, agent identity selection, onboarding preview,
  `init-agent`, controlled migration, strict verification, BotLand
  agent-specific auth, live identity probe, no-write smoke cycles, user
  systemd timer installation, generated unit review, enable/start commands,
  post-deployment health, remote sync pattern, pause/disable/rollback, service
  failure recovery, and a deployment checklist.
- Linked the guide from `docs/stay-alive/README.md`,
  `docs/stay-alive/OPERATIONS.md`, `docs/INDEX.md`, and
  `skills/stay-alive/SKILL.md`.
- Updated the npm package README to point to the bundled deployment guide.
- Updated the package prepare script so ClawHub references include
  `DEPLOYMENT.md`.

Verification:

- `git diff --check` on touched docs and package metadata.
- `node --check packages/stay-alive/tools/prepare-package.mjs`
- `npm pack --dry-run` from `packages/stay-alive`, confirming the tarball
  includes `docs/stay-alive/DEPLOYMENT.md`.
- Fixed the package `check` script to point regression at the repository
  runtime root when run from `packages/stay-alive`.
- Fixed the memory-pro regression fixture stub to use ESM imports, so it also
  works under the package's `"type": "module"` boundary.
- `npm run check` from `packages/stay-alive`: 205/205 pass, local-only,
  `external_write=false`, `botland_send=false`.

External writes: none. BotLand sends/posts/replies: none.

## Release Packaging

The owner asked to publish Stay-Alive to GitHub, ClawHub, and npm.

Implemented:

- Added `packages/stay-alive/` as the release wrapper for npm and ClawHub.
- Distribution correction: on GitHub, Stay-Alive belongs under the BotLand
  project; on ClawHub, it is an independent skill; on npm, the package was
  renamed to `agent-stay-alive` after the public `stay-alive` name was found to
  be occupied by an unrelated package.
- The package is generated from canonical source paths before packing:
  - `scripts/stay-alive/`
  - `docs/stay-alive/`
  - `skills/stay-alive/`
- Runtime ledgers remain excluded; `.gitignore` now ignores top-level
  `runtime/` so real agent state is not accidentally committed.
- npm package name: `agent-stay-alive`.
- ClawHub slug: `stay-alive`.
- Added a `stay-alive` CLI wrapper with `list`, `docs`, `path`, and
  `<script> -- [args]` modes.

Verification:

- `npm pack --dry-run` from `packages/stay-alive`
- `node packages/stay-alive/bin/stay-alive.mjs list`
- `node packages/stay-alive/bin/stay-alive.mjs onboarding-template -- --agent package-smoke --json`
- `node scripts/stay-alive/preflight.mjs --agent lobster-duck --strict-onboarding --no-checkpoint --json`
- `node scripts/stay-alive/regression-suite.mjs --agent lobster-duck --json`

Runtime ledgers included in release artifacts: none.

## Code And Documentation Cleanup

The owner asked to organize Stay-Alive code and docs after the recent
capability, mutation protocol, lifecycle, and onboarding work.

Implemented:

- Added `docs/stay-alive/CODEMAP.md` as the stable category map for
  `scripts/stay-alive/` entrypoints.
- Added `scripts/stay-alive/README.md` with edit rules and quick verification
  commands for the script directory.
- Updated `docs/stay-alive/README.md` to point to the code map, include the
  2026-06-08 dev log range, and record the current capability grant,
  mutation-protocol, lifecycle-evolution, and cross-agent onboarding surfaces.
- Updated `docs/stay-alive/ARCHITECTURE.md` date and source-module navigation.
- Updated `docs/stay-alive/OPERATIONS.md` with a common verification bundle and
  a pointer to the code map.
- Updated `docs/stay-alive/ROADMAP.md` current operating validation from the old
  2026-06-02 / 7-timer state to the 2026-06-08 / 8-timer state.
- Updated `docs/INDEX.md` to include current Stay-Alive docs and dev logs.
- Shortened `skills/stay-alive/SKILL.md` runtime layout from an exhaustive,
  duplicated script list to primary paths plus pointers to the maintained code
  map.

External writes: none. BotLand sends/posts/replies: none.
