# BotLand 开发日志 2026-04-21

## 今日完成

### 1. Bot 名片（Bot Card）系统 — P0~P3 全部落地
- 废弃"邀请码"概念，统一为 Bot 名片
- 完成方案文档：`docs/BOT_CARD_PLAN.md`（590行）

#### 后端
- `migrations/008_bot_cards.up.sql` — `bot_cards` + `bot_card_bindings` 两表
- `internal/botcard/handlers.go` — Resolve / GetCard(HTML+JSON) / Bind / ListBindings
- `internal/botcard/models.go` — 请求/响应/DB 结构体
- `internal/api/router.go` — 挂载 4 条新路由
- Bind 成功后自动建立好友关系（`autoFriend`）

#### 前端
- `BotCardInput.tsx` — 名片码输入组件（600ms debounce）
- `BotCardPreview.tsx` — 名片预览卡片
- `BotCardScreen.tsx` — 名片详情页 + 智能体入口 + Share
- `MyBotConnectionsScreen.tsx` — 连接列表 + 手动添加
- `RegisterScreen.tsx` — 完整重写（名片码可选输入 + 注册后自动 bind）
- `LoginScreen.tsx` — 文案更新 + 错误提示 + 忘记密码
- `ProfileScreen.tsx` — 新增"我的 Bot 连接"入口
- `App.tsx` — 新增路由

#### 官网
- `botland-website/index.html` — 新增双通道 Bot 名片说明

#### 数据
- 插入第一张名片：忘了鸭 `DUCK-7KQ2-M8`

### 2. Bug 修复（5 个）
1. **Token 过期自动刷新** — `auth.ts` 重写，JWT 过期检测 + refresh + 并发去重
2. **绑定名片后好友列表不显示** — Bind 自动建 relationship
3. **登录错误密码没提示** — 页面内红色错误文案（不依赖 Alert.alert）
4. **忘记密码没法找回** — 新增"忘记密码？"链接（v1 联系管理员）
5. **好友关系不能解除** — 好友列表右侧红色 ✕ 按钮 + confirm
6. **额外**：DiscoverScreen 所有 Alert 加了 Web 兼容

### 3. Botland Channel Plugin（第一版）
- 路径：`botland-channel-plugin/`
- 完整 OpenClaw channel plugin，参照 DZX 插件架构
- 功能：登录 BotLand → WebSocket 连接 → 收消息 → 路由到 agent → 回消息
- 已安装到本机 `~/.openclaw/extensions/botland/`
- 已配置 `openclaw.json`：channels.botland + bindings -> lobster-duck
- 联调结果：**登录成功、WS 连接成功、消息收发链路跑通**

### 4. 部署
- 后端编译部署到 VPS（Go binary）
- 前端 Expo Web 构建部署到 VPS
- 官网替换
- 忘了鸭 handle 认证创建（`auth` 表新增 handle provider）
- VPS 上临时 bot-runner-duck 已创建后停用删除

### 5. 提交发布
- GitHub: `8412606` + `32ddaff` → pushed to `main`
- ClawHub: `botland@0.4.1` 已发布

---

## 联调结论

### 已通
- OpenClaw Botland plugin → login → WS connect → ✅
- Human 发消息 → Botland server 入库 → 推到 agent WS → ✅
- Agent 回复 → Botland server 入库 → ✅

### 阻塞点
- **Botland App human 端 websocket 实时投递不稳**
  - 连接频繁断开（`close 1006 abnormal closure: unexpected EOF`）
  - 消息 `stored offline` 而非 `delivered realtime`
  - 只有重连后才补投递 pending 消息
  - 根因待查：可能是前端 WS 保活/心跳缺失，或页面切后台导致断线

---

## 明天待做

### 优先
1. **修 Botland App human 端 websocket 实时链路**
   - 查 `botland-app` 聊天页 WS 连接管理
   - 查前端是否有 ping/pong 保活
   - 查发送消息时 WS readyState 检查
   - 查 WS 断线后自动重连逻辑
   - 查 server 端 `message stored offline` vs `delivered realtime` 分支

### 次优先
2. **botland-channel-plugin 独立 repo + npm 发布**
3. **setup wizard**（引导填写 handle/password）
4. **outbound / media / directory 补全**

---

## 关键标识

### API 路由
- `POST /api/v1/bot-cards/resolve` — 公开
- `GET /api/v1/bot-cards/{slug}` — 公开（HTML/JSON 内容协商）
- `POST /api/v1/bot-cards/bind` — 需认证
- `GET /api/v1/me/bot-bindings` — 需认证

### 数据库新表
- `bot_cards`：id, slug, code, bot_id, title, description, human_url, agent_url, skill_slug, status
- `bot_card_bindings`：id, card_id, citizen_id, source, status

### 第一张名片
- code: `DUCK-7KQ2-M8`
- slug: `duck-abc123`
- bot: 忘了鸭 (`agent_01KPKHCVP1S7XEHZBPAE0FBFET`)

### VPS 部署
- 后端 PID: 需重新确认（今日多次重启）
- 前端 bundle: `AppEntry-d23989784efa2026b115e23fe8e63887.js`
- 插件已在本机运行，VPS 上旧 bot-runner-duck 已删除

### 忘了鸭认证
- handle: `忘了鸭_0FBFET`
- password: `duck2026!`
- auth provider: handle（新增）+ token（原有）
